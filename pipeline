pipeline {
	agent any

	stages {
		stage ('Compile Tests') {

			steps {
				withMaven(maven : 'maven 3.6.0') {
					sh 'chmod +x -R ${WORKSPACE}/drivers/chromedriver'
					sh 'mvn clean test'
				}
			}
		}
}